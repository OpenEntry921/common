import Link from "next/link";
import { ArrowRight, CalendarDays, Clock, MapPin, Users } from "lucide-react";
import type { CommonWeekStatus, Program, Space } from "@/lib/community-data";

export function DemoBadge(){return <span className="demo-label">DEMO · 예시 데이터</span>}
export function ProgramCard({program,status}:{program:Program;status?:CommonWeekStatus}){return <article className="card program-card"><div className="program-card-labels"><DemoBadge/>{status&&status!=="current"&&<span className="week-status">{status==="past"?"종료":"예정"}</span>}</div><h3>{program.title}</h3><p className="card-copy">{program.description}</p><div className="details details-single"><span><CalendarDays size={14}/>{program.date}</span><span><Clock size={14}/>{program.time}</span><span><MapPin size={14}/>{program.place}</span><span><Users size={14}/>{program.capacity}</span></div><div className="meta-list"><span>{program.fee}</span><span>{program.application}</span><span>HOST · {program.host}</span>{program.openSeats&&<strong>{program.openSeats}</strong>}</div></article>}
export function SpaceCard({space}:{space:Space}){return <article className="card space-card"><div className="space-placeholder" aria-label="공간 사진 준비 중">공간 사진 준비 중</div><DemoBadge/><h3>{space.name}</h3><p className="card-copy">{space.description}</p><p><b>{space.recommended}</b> · {space.maximum}</p><div className="tag-row">{space.uses.map(x=><span className="tag" key={x}>{x}</span>)}</div><p className="microcopy">{space.availability}<br/>{space.fee}</p><Link className="button secondary" href={`/space/${space.slug}`}>공간 자세히 보기 <ArrowRight size={16}/></Link></article>}

import Link from "next/link";import { PageHead } from "@/components/UI";import { circleDemo, contributionDemo } from "@/lib/community-data";
export default function Page(){const stats=[[circleDemo.uses,"공간 이용"],[circleDemo.income,"공간 사용료"],[circleDemo.cost,"실제 운영비"],[circleDemo.contribution,"지역사회 환원"],[circleDemo.cumulative,"누적 환원"]];return <><PageHead eyebrow="OUR IMPACT" title="함께 만든 변화" desc="이 공간에서 시작된 가치가 어떻게 다시 동네로 돌아가는지 확인해보세요."/><section className="section"><div className="container"><div className="honesty-note"><b>DEMO · 아래 숫자는 실제 운영 실적이 아닙니다.</b><p>실제 공간 운영과 환원이 시작되면 검증 가능한 금액·날짜·기관·증빙만 공개합니다.</p></div><p className="eyebrow">{circleDemo.period}</p><div className="grid impact-grid">{stats.map(([n,l])=><article className="card" key={l}><p className="impact-number">{n}</p><b>{l}</b><p className="microcopy">시연용 예시</p></article>)}</div><section className="records"><div className="section-intro"><div><p className="eyebrow">CONTRIBUTION RECORD</p><h2 className="section-title">환원 내역</h2></div></div>{contributionDemo.map(r=><article className="card record" key={r.date}><span className="demo-label">DEMO · 실제 집행 아님</span><div><small>{r.date}</small><h3>{r.category}</h3><p>{r.purpose}</p></div><div><b>{r.amount}</b><p>{r.organization}</p><small>{r.status} · {r.evidence}</small></div></article>)}</section><p className="privacy-note">개별 수혜자의 이름·연락처·주소·가정사 등 개인정보는 공개하지 않습니다.</p><Link className="button" href="/circle">COMMON CIRCLE 원칙 보기</Link></div></section></>}

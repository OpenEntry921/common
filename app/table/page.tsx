import { PageHead } from "@/components/UI";import { commonTables } from "@/lib/community-data";
export default function Page(){return <><PageHead eyebrow="TABLE" title="COMMON TABLE" desc="오늘 당신에게 맞는 자리를 찾아보세요."/><section className="section"><div className="container"><div className="grid grid-2">{commonTables.map(t=><article className="card table-card" key={t.name}><span className="table-icon">{t.icon}</span><p className="eyebrow">{t.name}</p><h2>{t.copy}</h2><p>{t.description}</p></article>)}</div><div className="honesty-note table-note"><b>TABLE은 물리적인 자리 경험입니다.</b><p>상담이나 전문가 연결을 제공하는 좌석이 아닙니다. 도움이 어디에 있는지 찾고 싶다면 별도의 COMMON CONNECT를 이용해주세요. 실제 테이블 운영 방식과 표시는 준비 후 안내합니다.</p></div></div></section></>}

import Link from "next/link";import { PageHead } from "@/components/UI";import { SpaceCard } from "@/components/CommunityCards";import { commonSpaces } from "@/lib/community-data";
export default function Page(){return <><PageHead eyebrow="SPACE" title="COMMON SPACE" desc={'좋은 모임에는 좋은 공간이 필요하니까요.\n회의, 공부, 작은 클래스, 동아리, 이웃과의 모임까지. 필요한 만큼 편안하게 사용하세요.'}/><section className="section"><div className="container"><div className="honesty-note"><b>공간 정보 확인 중</b><p>실제 2층 공간명·사진·수용인원·이용 시간·사용료가 확정되기 전까지 모든 공간 카드는 DEMO로 표시합니다.</p></div><div className="grid grid-2">{commonSpaces.map(s=><SpaceCard key={s.slug} space={s}/>)}</div><div className="space-entry"><div><p className="eyebrow">COMMON TABLE</p><h2>오늘 나에게 맞는 자리 찾기</h2><p>조용히 머물거나 이웃과 자연스럽게 만나는 공간 경험을 살펴보세요.</p></div><Link className="button secondary" href="/table">COMMON TABLE 보기</Link></div><p className="circle-promise">공간 운영에 필요한 실제 비용을 제외한 금액은 COMMON의 지역사회 환원 원칙에 따라 사용됩니다.</p></div></section></>}

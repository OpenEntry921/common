"use client";
import { PageHead,Toast } from "@/components/UI";import { marketPosts } from "@/lib/data";import { useState } from "react";
export default function Page(){const [toast,setToast]=useState(false);return <><PageHead eyebrow="NEIGHBORHOOD MARKET" title="COMMON 나눔 장터" desc="쓰지 않는 물건을 나누고, 잠시 필요한 것을 빌리고, 함께할 이웃을 만나요."/><section className="section"><div className="container"><div className="grid grid-3">{marketPosts.map(([tag,title,desc])=><article className="card post-card" key={title}><span className="tag">{tag}</span><h3>{title}</h3><p>{desc}</p><div className="post-meta"><small>COMMON 이웃 · 최근</small><button className="small-button primary" onClick={()=>{setToast(true);setTimeout(()=>setToast(false),2200)}}>관심 있어요</button></div></article>)}</div><div className="notice" style={{marginTop:30}}>안전한 나눔을 위해 공개된 장소에서 만나고, 금전 거래보다 이웃 간 공유를 우선해 주세요. 모든 게시물은 데모입니다.</div></div></section>{toast&&<Toast message="관심을 보냈어요. 데모 알림입니다."/>}</>}

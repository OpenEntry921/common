"use client";import { useState } from "react";import { PageHead,Success } from "@/components/UI";
const reasons=["그냥 이야기를 들어줄 사람이 필요해요","신앙에 대해 궁금한 것이 있어요","기도를 받고 싶어요","생활에 도움이 필요해요","목회 상담을 원해요"];
export default function Page(){const [done,setDone]=useState(false);return <><PageHead eyebrow="TALK TO SOMEONE" title="사람과 이야기하고 싶어요" desc="AI가 아니라 실제 사람과 이야기하고 싶다면 연결을 요청할 수 있습니다. 원하지 않을 때 언제든 연결을 종료할 수 있어요."/><section className="section"><div className="container" style={{maxWidth:650}}>{done?<Success title="연결 요청을 확인했습니다." desc="선택한 방법으로 가능한 시간을 안내드릴게요.\n데모에서는 실제 연락이 진행되지 않습니다." onDone={()=>setDone(false)}/>:<form className="form" onSubmit={e=>{e.preventDefault();setDone(true)}}><div className="field"><label>어떤 이야기를 나누고 싶나요?</label><select required defaultValue=""><option value="" disabled>선택해 주세요</option>{reasons.map(x=><option key={x}>{x}</option>)}</select></div><p className="label">편한 연락 방법</p><div className="choice-grid">{["문자","전화","카카오톡","직접 만나기"].map(x=><label className="choice" key={x}><input name="contact" type="radio" required/>{x}</label>)}</div><div className="field"><label>연락처와 편한 시간</label><input required placeholder="예: 010-0000-0000, 평일 저녁"/></div><div className="notice">상담 내용은 공개하지 않으며 연결에 필요한 정보만 확인합니다. 이 프로토타입에서는 입력한 정보가 저장되거나 전송되지 않습니다.</div><button className="button">사람 연결 요청하기</button></form>}</div></section></>}

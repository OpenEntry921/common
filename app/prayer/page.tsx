"use client";import { useState } from "react";import { PageHead,Success } from "@/components/UI";
export default function Page(){const [done,setDone]=useState(false);return <><PageHead eyebrow="PRAYER REQUEST" title="기도를 부탁하고 싶어요" desc="누군가가 당신을 위해 기도해 주었으면 하는 일이 있나요? 공개 범위를 직접 선택할 수 있습니다."/><section className="section"><div className="container" style={{maxWidth:650}}>{done?<Success title="소중한 이야기를 남겨주셔서 감사합니다." desc="당신의 이야기를 가볍게 여기지 않겠습니다.\n이 화면은 데모이며 입력 내용은 저장되지 않았습니다." onDone={()=>setDone(false)}/>:<form className="form" onSubmit={e=>{e.preventDefault();setDone(true)}}><p className="label">공개 및 연락 범위</p>{["완전 익명","이름 일부 공개","연락을 받아도 됨"].map((x,i)=><label className="choice" key={x}><input name="privacy" type="radio" required defaultChecked={i===0}/>{x}</label>)}<div className="field"><label>기도 제목을 남겨주세요.</label><textarea required placeholder="편안한 말로 적어주세요. 자세히 쓰지 않아도 괜찮습니다."/></div><div className="notice">기도에 필요한 최소한의 내용만 남겨주세요. 공개 여부는 언제든 바꿀 수 있고, 민감한 내용은 커뮤니티나 관리자 통계에 표시하지 않습니다.</div><button className="button">기도 부탁 남기기</button></form>}</div></section></>}
